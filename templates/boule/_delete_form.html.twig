<form method="post" action="{{ path('app_boule_delete', {'id': boule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boule.id) }}">
    <button class="btn">Delete</button>
</form>
